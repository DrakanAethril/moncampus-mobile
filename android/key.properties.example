# Template for android/key.properties (gitignored, never committed - see .gitignore).
# Generated once by the release-mobile-apps skill into
# /Users/Shared/Beaupeyrat/Secrets/mobile-signing/moncampus-key.properties and copied here as
# android/key.properties. Needed only for `flutter build apk --release`; debug/profile builds and
# `flutter run` don't touch this file.
storePassword=
keyPassword=
keyAlias=release
storeFile=/absolute/path/to/moncampus-release.jks
